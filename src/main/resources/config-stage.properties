# ========================================
# STAGE Environment Configuration
# ========================================

# Environment Information
environment.name=STAGE
environment.description=Staging/Pre-Production Environment
environment.color=yellow

# Application URLs
app.url=https://stage.thegroup.com.qa
app.url.en=${app.url}/en/trading
app.url.ar=${app.url}/ar/trading
app.login.url=${app.url}/en/login

# Test Credentials (STAGE)
username=stage_testuser
password=Stage@123
test.client.id=12240
test.nin=12240

# Database Configuration (STAGE)
db.url=DB01S:1523/GRPSTAGE
db.user=sec1
db.password=sec12345
db.schema=SEC1

# API Configuration (STAGE)
api.base.url=https://api-stage.thegroup.com.qa
api.timeout=20000
api.retry.count=2

# Browser Configuration
browser.default=CHROME
browser.headless=false
browser.maximize=true
browser.clear.cookies=true
browser.delete.all.cookies=true

# Wait Times (in seconds) - Production-like
wait.implicit=10
wait.explicit=12
wait.page.load=25
wait.short=2
wait.medium=6
wait.long=12

# Screenshot Configuration
screenshot.on.failure=true
screenshot.on.success=false
screenshot.path=target/screenshots/stage/
screenshot.format=PNG

# Logging Configuration
log.level=INFO
log.path=target/logs/stage/
log.file.name=stage-test-execution.log

# Retry Configuration
retry.failed.tests=true
retry.max.count=1

# Parallel Execution
parallel.execution=true
thread.count=4

# Test Data Paths
testdata.excel.path=src/test/resources/testdata/stage/TestData.xlsx
testdata.json.path=src/test/resources/testdata/stage/testdata.json
testdata.csv.path=src/test/resources/testdata/stage/

# Report Configuration
report.path=target/reports/stage/
allure.results.directory=target/allure-results/stage/

# Performance Thresholds (STAGE - strict)
performance.page.load.threshold=2500
performance.api.response.threshold=1000

# Feature Flags (STAGE - production-like)
feature.new.ui.enabled=true
feature.beta.trading.enabled=false
feature.advanced.charts.enabled=true

# Email Configuration (STAGE)
email.notifications.enabled=true
email.recipients=stage-team@example.com,qa-lead@example.com

# Timeouts (in milliseconds)
timeout.connection=30000
timeout.socket=30000
timeout.request=25000

# Debug Settings
debug.mode=false
debug.screenshots=false
debug.verbose.logging=false
debug.save.page.source=false
