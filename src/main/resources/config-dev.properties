# ========================================
# DEV Environment Configuration
# ========================================

# Environment Information
environment.name=DEV
environment.description=Development Environment
environment.color=blue

# Application URLs
app.url=https://dev.thegroup.com.qa
app.url.en=${app.url}/en/trading
app.url.ar=${app.url}/ar/trading
app.login.url=${app.url}/en/login

# Test Credentials (DEV)
username=dev_testuser
password=Dev@123
test.client.id=10001
test.nin=10001

# Database Configuration (DEV)
db.url=DB01D:1523/GRPDEV
db.user=sec1
db.password=sec12345
db.schema=SEC1

# API Configuration (DEV)
api.base.url=https://api-dev.thegroup.com.qa
api.timeout=30000
api.retry.count=3

# Browser Configuration
browser.default=CHROME
browser.headless=false
browser.maximize=true
browser.clear.cookies=true
browser.delete.all.cookies=true

# Wait Times (in seconds) - More relaxed for DEV
wait.implicit=15
wait.explicit=20
wait.page.load=45
wait.short=5
wait.medium=10
wait.long=20

# Screenshot Configuration
screenshot.on.failure=true
screenshot.on.success=false
screenshot.path=target/screenshots/dev/
screenshot.format=PNG

# Logging Configuration
log.level=DEBUG
log.path=target/logs/dev/
log.file.name=dev-test-execution.log

# Retry Configuration
retry.failed.tests=true
retry.max.count=3

# Parallel Execution
parallel.execution=false
thread.count=2

# Test Data Paths
testdata.excel.path=src/test/resources/testdata/dev/TestData.xlsx
testdata.json.path=src/test/resources/testdata/dev/testdata.json
testdata.csv.path=src/test/resources/testdata/dev/

# Report Configuration
report.path=target/reports/dev/
allure.results.directory=target/allure-results/dev/

# Performance Thresholds (DEV - relaxed)
performance.page.load.threshold=5000
performance.api.response.threshold=2000

# Feature Flags (DEV)
feature.new.ui.enabled=true
feature.beta.trading.enabled=true
feature.advanced.charts.enabled=true

# Email Configuration (DEV)
email.notifications.enabled=false
email.recipients=dev-team@example.com

# Timeouts (in milliseconds)
timeout.connection=60000
timeout.socket=60000
timeout.request=45000

# Debug Settings
debug.mode=true
debug.screenshots=true
debug.verbose.logging=true
debug.save.page.source=true
